#include "main.h"

/**
 * _isdigit - If it is from 0 to 9 return 1, else return 0
 * @c: char to be entered
 * Return: 0
 **/

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
